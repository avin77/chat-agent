# Plan 15-02: Universal PII & Organization

**Requirement ID:** FLY-06
**One-liner:** Standardize output directory structure and privacy scrubbing.

## Goal
Implement the date-stamped subdirectory structure and global PII redaction as decided in the context phase.

## Tasks
1. **Universal PII Utility**: Create `src/lib/agentic/piiUtils.ts`.
    - Implement regex-based masking for phone numbers, email addresses, and specific name patterns.
    - Ensure it is usable by both server-side logic and offline scripts.
2. **Directory Management**: Refactor `scripts/lib/flywheelUtils.js` to support:
    - `data/mined/{intent}/` path resolution.
    - `golden-{YYYY-MM-DD}.json` filename generation.
3. **Script Integration**: Update all mining scripts to use the new `piiUtils` and the refined path generation logic.

## Verification
- Verify `data/mined/complaint/` and `data/mined/maid_hire/` folders are created.
- Verify file names follow the `golden-YYYY-MM-DD.json` pattern.
- Confirm all output data has PII replaced with `[REDACTED]`.
