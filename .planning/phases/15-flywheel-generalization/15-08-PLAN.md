# Plan 15-08: Dashboard Metrics Wiring

**Requirement ID:** FLY-07
**One-liner:** Enable cross-intent comparisons in the PM UI.

## Goal
Surface the generalized flywheel performance metrics to the PM Dashboard.

## Tasks
1. **Metric Aggregation**: Update `src/app/dashboard/actions.ts` -> `getProductHealthMetrics()`.
    - Group metrics (Completion Rate, Lead Quality) by `detected_intent`.
2. **Charts Update**: Refactor dashboard charts to support intent-based filtering.
    - Allow PMs to compare `maid_hire` performance vs `complaint` performance side-by-side.

## Verification
- Verify the "Overview" and "Agentic Quality" tabs now allow filtering by intent.
- Confirm metrics correctly reflect the generalized data mined in previous waves.
