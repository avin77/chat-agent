# Plan 15-07: Advanced Eval Suite

**Requirement ID:** AG-L3, AG-REFL
**One-liner:** Stress test the new autonomy features with complex edge cases.

## Goal
Expand the automated evaluation suites to verify the specific Level 3 behaviors like intent pivoting and history audits.

## Tasks
1. **Unhappy Dataset Update**: Add 5-10 new cases to `data/unhappy-golden-dataset.json` focused on:
    - User saying "I already gave you that."
    - User switching from a Hire to a Complaint mid-convo.
    - User being vague about location.
2. **Eval Logic Update**: Update `eval-state-machine.js` to verify that the `handledIntent` correctly switches when a pivot is detected.

## Verification
- Run `npm run eval:unhappy` and verify the agent handles these reasoning-heavy cases with > 90% accuracy.
