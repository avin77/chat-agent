# Plan 15-06: UX Visibility

**Requirement ID:** UX-PROC
**One-liner:** Surface agentic reasoning to users and PMs.

## Goal
Manage user expectations during reasoning latency and provide full transparency to PMs in the dashboard.

## Tasks
1. **UI Indicator**: Update `src/app/chat/page.tsx` (or related message component).
    - Add a "Thinking..." or "Processing..." state that displays while the agent is in the reflection loop.
2. **Dashboard Upgrade**: Update the "Agentic Quality" tab in the dashboard.
    - Add a display for the `thought_reflection` string in the conversation log view.
    - Ensure PMs can see the "Full Chain of Thought" for every audited turn.

## Verification
- Manual verification: Send a message and confirm the "Processing" indicator appears.
- Manual verification: Open Dashboard and confirm the `thought_reflection` string is visible for recent turns.
