---
phase: 03-dashboard-cost-tracking
plan: "05"
type: execute
wave: 4
depends_on:
  - "03-04"
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - SHADOW-02
  - SHADOW-03
  - SHADOW-04
must_haves:
  truths:
    - "Supabase migration has been run and all 3 schema changes are live"
    - "Product Health tab shows real metrics (not empty)"
    - "Chat conversation produces token data in llm_logs"
    - "npm run eval:state passes >= 95% (no regressions from Phase 3 changes)"
  artifacts:
    - path: "supabase-migration-phase3.sql"
      provides: "Migration file that was executed in Supabase SQL Editor"
      contains: "ALTER TABLE llm_logs"
  key_links:
    - from: "Supabase SQL Editor"
      to: "llm_logs table"
      via: "Manual execution of supabase-migration-phase3.sql"
      pattern: "prompt_tokens column exists"
---

<objective>
Execute the Supabase migration and verify the complete Phase 3 system end-to-end: token logging, dashboard Product Health tab, shadow handler, intent classifier, and alert system.

Purpose: Final gate before Phase 3 is considered complete. Ensures the migration SQL runs without errors and all Phase 3 features work together in the live environment.

Output: Confirmed working Phase 3 system with migration applied and all features verified.
</objective>

<execution_context>
@C:/Users/shobh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-cost-tracking/03-CONTEXT.md
@.planning/phases/03-dashboard-cost-tracking/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and eval verification</name>
  <files></files>
  <action>
Run the production build and eval suite to verify no regressions:

1. Run TypeScript check: `npx tsc --noEmit`
2. Run production build: `npm run build`
3. Run eval against state golden dataset: `npm run eval:state` (or `npm run eval` if :state isn't available — check package.json)

If eval score is < 95%, investigate which conversations failed and fix before proceeding. The intent classifier adds a 3rd Gemini call per maid_hire turn — if rate limiting is causing failures, update the rate limiter's recordRequest() call in the classifier section of route.ts to only record when actually calling the classifier.

Verify the build output includes no new TypeScript errors related to Phase 3 files:
- src/extractors/intentClassifier.ts
- src/lib/shadowHandler.ts
- src/app/dashboard/actions.ts
- src/app/dashboard/page.tsx
- src/lib/llm-logger.ts
- src/app/api/chat/route.ts
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    npm run build passes.
    TypeScript compiles with no errors.
    Eval score is >= 95% (or documented reason if lower with fix plan).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 system:
- supabase-migration-phase3.sql ready to run (4 token columns + shadow_logs + system_alerts tables)
- Token logging wired into route.ts (captures usage.inputTokens / usage.outputTokens from generateText)
- logLLMInteraction() extended with promptTokens, completionTokens, totalTokens, estimatedCostUsd
- Intent classifier (src/extractors/intentClassifier.ts) integrated into maid_hire flow
- Shadow handler (src/lib/shadowHandler.ts) fires async after each maid_hire turn
- Dashboard Product Health tab populated with KPI cards, slot fill rates, token cost, shadow panel, alert banner
- Alert system: checkAndWriteAlerts() checks fallback rate, LLM error rate, eval regression, shadow alignment
  </what-built>
  <how-to-verify>
**Step 1: Run the Supabase migration**
1. Open Supabase dashboard → SQL Editor
2. Copy the contents of `supabase-migration-phase3.sql` from the project root
3. Paste and run it
4. Confirm: no errors, "Success" message
5. Go to Table Editor — verify `llm_logs` has new columns: prompt_tokens, completion_tokens, total_tokens, estimated_cost_usd
6. Verify `shadow_logs` table exists
7. Verify `system_alerts` table exists

**Step 2: Test token logging**
1. Start dev server: `npm run dev`
2. Open http://localhost:3000
3. Send a maid hire message (e.g. "I need a maid in Koramangala")
4. Go to Supabase → llm_logs table → most recent row
5. Confirm: prompt_tokens, completion_tokens, total_tokens are non-null integers
6. Confirm: estimated_cost_usd is 0 (Gemma is free)

**Step 3: Verify Product Health tab**
1. Open http://localhost:3000/dashboard
2. Click "Product Health" tab
3. Confirm: tab has content (not blank)
4. Confirm: KPI cards visible (Lead Completion, Lead Quality Score, Effective Escalation, Avg Session Duration)
5. Confirm: Slot Fill Rates section shows 7 fields with bars
6. Confirm: Token Usage section shows "No token data yet" OR shows token numbers
7. Confirm: Shadow Mode Alignment panel shows "No shadow data yet" (expected — no conversations with shadow active)

**Step 4: Test intent classifier (optional — observation only)**
In a maid_hire conversation, send 2 completely off-topic messages like "Who won the cricket match?" twice in a row at the same state. The bot should respond with an offer to restart or connect to support (not just re-ask the question).

**Step 5: Run eval**
```
npm run eval:state
```
Score must be >= 95%. If lower, document the failures.
  </how-to-verify>
  <resume-signal>
Type "approved" if all steps pass.
Or describe issues found (e.g. "Supabase migration failed: [error]", "Product Health tab still empty", "eval score dropped to 92% on test X").
  </resume-signal>
</task>

</tasks>

<verification>
All 5 verification steps above must pass:
1. Supabase migration runs without errors
2. llm_logs rows have non-null token columns after a conversation
3. Product Health tab renders content
4. Shadow panel shows (even if empty)
5. eval score >= 95%
</verification>

<success_criteria>
Supabase migration applied: prompt_tokens, completion_tokens, total_tokens, estimated_cost_usd columns in llm_logs; shadow_logs table; system_alerts table.
After one maid_hire conversation: llm_logs row has non-null token data.
Dashboard Product Health tab shows live data.
npm run build passes.
Eval score >= 95% (no regressions from Phase 3 changes).
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-cost-tracking/03-05-SUMMARY.md`
</output>
