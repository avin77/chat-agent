---
phase: 10-v3-06-eval-governance-3-tracks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/evalGovernance.ts
  - scripts/check-eval-governance.js
  - package.json
  - src/test/test-eval-governance.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Release decisions are derived from per-track policy, not a blended average score"
    - "Known blocker slices such as c15, c28, and c56 are encoded in one reusable contract"
    - "Missing required eval artifacts block release rather than silently degrading to a pass"
    - "CLI tooling and later dashboard wiring consume the same governance helper"
  artifacts:
    - path: "src/lib/evalGovernance.ts"
      provides: "Typed eval governance contract and release verdict helpers"
      exports: ["EVAL_GOVERNANCE_POLICY", "evaluateEvalGovernance", "classifyEvalTrack"]
    - path: "scripts/check-eval-governance.js"
      provides: "Local checker for latest eval artifacts with readable and JSON output"
      contains: "--json"
    - path: "src/test/test-eval-governance.ts"
      provides: "Focused regression tests for thresholds, blocker slices, and missing-track behavior"
      contains: "c56"
  key_links:
    - from: "scripts/check-eval-governance.js"
      to: "src/lib/evalGovernance.ts"
      via: "shared policy and verdict helpers"
      pattern: "evaluateEvalGovernance"
    - from: "src/test/test-eval-governance.ts"
      to: "src/lib/evalGovernance.ts"
      via: "contract-level tests"
      pattern: "EVAL_GOVERNANCE_POLICY"
---

<objective>
Create the canonical eval governance contract and local release checker for Phase 10.

Purpose: move release gating out of ad hoc dashboard thresholds and into one reusable policy that evaluates `eval`, `eval:state`, and `eval:unhappy` artifacts consistently.

Output:
- shared governance helper
- local governance checker script
- package scripts
- focused policy tests
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-v3-06-eval-governance-3-tracks/10-RESEARCH.md
@package.json
@src/app/dashboard/actions.ts
@src/app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared eval governance contract</name>
  <files>src/lib/evalGovernance.ts</files>
  <action>
Create `src/lib/evalGovernance.ts` as the canonical source of truth for Phase 10 release policy.

The file must:
- define track ids for `normal`, `state`, and `unhappy`
- encode initial policy floors
  - `state >= 95`
  - `unhappy >= 90`
  - `normal >= 95`
- encode known blockers from roadmap evidence
  - `state`: `c15`, `c28`
  - `unhappy`: `c56`
- treat all three tracks as required unless the caller explicitly overrides policy
- expose helpers to:
  - classify eval files by track from filename
  - normalize a track result from an eval JSON file
  - evaluate track pass / fail / blocked state
  - compute final release verdict with reasons

Return a structured verdict object that includes:
- `releaseVerdict`
- `tracks[]`
- `blockingReasons[]`
- `warningReasons[]`
- `latestFiles`

Do not couple this file to Next.js or Supabase. Keep it pure and importable from both Node scripts and app code.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>shared governance helper exists, compiles, and exposes reusable typed policy helpers for all three eval tracks</done>
</task>

<task type="auto">
  <name>Task 2: Add a local governance checker and package scripts</name>
  <files>scripts/check-eval-governance.js, package.json</files>
  <action>
Add `scripts/check-eval-governance.js` to evaluate the latest available eval artifacts under `data/`.

Requirements:
- resolve the latest file for each track:
  - `eval-state-*.json` -> `state`
  - `eval-unhappy-*.json` -> `unhappy`
  - `eval-*.json` excluding state and unhappy -> `normal`
- read each file safely
- call the shared helper from `src/lib/evalGovernance.ts`
- print a readable summary showing:
  - file used
  - score
  - floor
  - blocker slice status
  - final release verdict
- support `--json`
- support optional explicit overrides:
  - `--state <file>`
  - `--unhappy <file>`
  - `--normal <file>`

Update `package.json` with at least:
- `eval:governance`
- `eval:governance:json`

Do not duplicate policy constants in the script.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node --experimental-strip-types scripts/check-eval-governance.js --json</automated>
  </verify>
  <done>the repo has a runnable governance checker that evaluates latest track artifacts without duplicating release policy</done>
</task>

<task type="auto">
  <name>Task 3: Lock the governance contract with focused tests</name>
  <files>src/test/test-eval-governance.ts</files>
  <action>
Add focused tests for the shared governance helper.

Cover at minimum:
- all tracks present and above floor -> release passes
- `unhappy` below floor -> release blocks
- `c56` blocker present despite score above floor -> release blocks
- missing `normal` track -> release blocks
- file classification does not misclassify `eval-state-*` as normal

Use lightweight Node-based tests consistent with the existing `src/test` setup.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node --experimental-strip-types src/test/test-eval-governance.ts</automated>
  </verify>
  <done>policy tests cover threshold, blocker-slice, and missing-artifact failure modes</done>
</task>

</tasks>

<verification>
- one shared governance helper exists for `normal`, `state`, and `unhappy`
- latest eval files can be classified and evaluated locally
- blocker slices are encoded in data, not only docs
- package scripts expose governance checking
- focused tests validate the core release-decision rules
</verification>

<success_criteria>
- repo has a single source of truth for eval governance
- release cannot pass on overall score alone
- `c56`-style known blockers are first-class policy inputs
- future dashboard and CI wiring can consume the same verdict object
</success_criteria>

<output>
After completion, create `.planning/phases/10-v3-06-eval-governance-3-tracks/10-01-SUMMARY.md`
</output>
