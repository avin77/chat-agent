---
phase: 10-v3-06-eval-governance-3-tracks
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/dashboard/actions.ts
  - src/app/dashboard/page.tsx
  - docs/eval-governance.md
  - src/test/test-dashboard-eval-governance.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "The dashboard pre-production view reads the same release verdict that the local governance checker uses"
    - "PM can see which track or blocker slice is preventing a release"
    - "Missing eval artifacts render as an explicit blocked state, not as an empty or misleading green checklist"
    - "Governance policy is documented in PM-readable language without creating a second source of truth"
  artifacts:
    - path: "src/app/dashboard/actions.ts"
      provides: "Server action that loads latest eval files and returns the shared governance verdict"
      contains: "getEvalGovernanceStatus"
    - path: "src/app/dashboard/page.tsx"
      provides: "Pre-production checklist wired to shared governance output"
      contains: "release verdict"
    - path: "docs/eval-governance.md"
      provides: "PM-facing explanation of track floors, blocker slices, and how to interpret failures"
      contains: "c15, c28, c56"
  key_links:
    - from: "src/app/dashboard/actions.ts"
      to: "src/lib/evalGovernance.ts"
      via: "shared policy evaluation"
      pattern: "evaluateEvalGovernance"
    - from: "src/app/dashboard/page.tsx"
      to: "src/app/dashboard/actions.ts"
      via: "server action consumption"
      pattern: "getEvalGovernanceStatus"
---

<objective>
Wire Phase 10 governance into the PM dashboard and document how releases are judged.

Purpose: make eval governance decision-enforcing for PM review instead of leaving it as a local-only script or a hardcoded UI checklist.

Output:
- dashboard server action for governance status
- pre-production checklist wired to the shared verdict
- PM-readable governance doc
- focused dashboard-facing tests
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-v3-06-eval-governance-3-tracks/10-RESEARCH.md
@.planning/phases/10-v3-06-eval-governance-3-tracks/10-01-PLAN.md
@src/lib/evalGovernance.ts
@src/app/dashboard/actions.ts
@src/app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose governance verdict through dashboard actions</name>
  <files>src/app/dashboard/actions.ts</files>
  <action>
Add `getEvalGovernanceStatus()` to `src/app/dashboard/actions.ts`.

Requirements:
- reuse the latest-file resolution pattern already used for eval track scores
- load `normal`, `state`, and `unhappy` artifacts
- call the shared `evaluateEvalGovernance()` helper
- return:
  - final release verdict
  - per-track score and file
  - blocker slice results
  - blocking reasons
  - warning reasons
- fail safely when a file is missing or malformed

Do not duplicate track thresholds or blocker ids in this file.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>dashboard actions expose a shared governance verdict with safe fallbacks and no policy duplication</done>
</task>

<task type="auto">
  <name>Task 2: Replace checklist hardcoding with governance-driven UI</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
Update the pre-production checklist and related eval-gate UI in `src/app/dashboard/page.tsx` so it renders from `getEvalGovernanceStatus()`.

Requirements:
- show one overall release verdict badge
- show per-track status rows for `normal`, `state`, and `unhappy`
- show blocker slices such as `c15`, `c28`, and `c56` when they are still failing
- show an explicit blocked state when a required artifact is missing
- keep the dashboard readable for PMs; do not expose raw JSON blobs directly

Do not leave fallback hardcoded threshold comparisons in JSX once the shared verdict exists.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>the dashboard pre-production section is driven by the shared governance verdict rather than separate local threshold logic</done>
</task>

<task type="auto">
  <name>Task 3: Add PM documentation and a dashboard-facing regression test</name>
  <files>docs/eval-governance.md, src/test/test-dashboard-eval-governance.ts</files>
  <action>
Create `docs/eval-governance.md` and a focused test covering the dashboard-facing contract.

Documentation must explain:
- the three eval tracks and why all are required
- initial floors
- known blocker slices
- what blocks a release versus what produces only a warning
- how to update the policy when a blocker class is resolved or a new one is discovered

Test coverage must include:
- missing-track result remains blocked
- blocker reasons surface in a human-readable shape suitable for dashboard display

Keep the doc aligned with `src/lib/evalGovernance.ts`; do not introduce conflicting thresholds.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node --experimental-strip-types src/test/test-dashboard-eval-governance.ts</automated>
  </verify>
  <done>PM docs exist and the dashboard-facing governance contract is protected by a focused regression test</done>
</task>

</tasks>

<verification>
- dashboard action returns the shared governance verdict
- checklist UI reads shared verdict output instead of hardcoded thresholds
- PM doc explains the release policy and blocker slices
- tests cover missing-track and blocker-surface behavior
</verification>

<success_criteria>
- PM can tell immediately whether a release is blocked and why
- governance logic is identical between local CLI checks and dashboard review
- track-specific failures are visible without reading raw eval JSON manually
</success_criteria>

<output>
After completion, create `.planning/phases/10-v3-06-eval-governance-3-tracks/10-02-SUMMARY.md`
</output>
