---
phase: 14-true-agentic-parity-agentic-orchestration-and-complete-agentic-based-system
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/flows/agenticMaidHire.ts
  - src/lib/shadowHandler.ts
  - src/app/api/chat/route.ts
  - src/extractors/dataExtractor.ts
  - src/lib/llm-logger.ts
  - src/test
autonomous: true
requirements: []

must_haves:
  truths:
    - "maid_hire live agentic behavior uses the shared runtime contract instead of a separate logic island"
    - "Shadow mode for maid_hire becomes a faithful simulation of the same runtime"
    - "Single out-of-order valid slots and multi-slot messages are both persisted without unnecessary re-asking"
    - "Wrong or ambiguous values are rejected with repair prompts, not silently committed"
  artifacts:
    - path: "src/flows/agenticMaidHire.ts"
      provides: "maid_hire adapter onto the shared runtime"
    - path: "src/lib/shadowHandler.ts"
      provides: "simulate-mode execution using the same runtime as production"
    - path: "src/extractors/dataExtractor.ts"
      provides: "cleaner extraction behavior for false positives like location phrases mistaken as names"
---

<objective>
Move maid_hire onto the shared agentic runtime and upgrade shadow mode from prompt-only simulation to faithful runtime simulation.

Purpose: make maid_hire the reference implementation for true agentic parity before other intents migrate.

Output:
- shared-runtime maid_hire path
- shared-runtime shadow simulation
- better out-of-order capture and repair behavior
- richer parity logging for PM inspection
</objective>

<context>
@.planning/phases/14-true-agentic-parity-agentic-orchestration-and-complete-agentic-based-system/14-CONTEXT.md
@.planning/phases/14-true-agentic-parity-agentic-orchestration-and-complete-agentic-based-system/14-RESEARCH.md
@.planning/phases/14-true-agentic-parity-agentic-orchestration-and-complete-agentic-based-system/14-01-PLAN.md
@src/flows/agenticMaidHire.ts
@src/lib/shadowHandler.ts
@src/flows/BaseFlow.ts
@src/extractors/dataExtractor.ts
@src/app/api/chat/route.ts
</context>

<tasks>

<task>
  <name>Migrate maid_hire live agentic path to the shared runtime</name>
  <action>
Refactor the current maid_hire agentic handler so it becomes an adapter around the shared runtime rather than owning its own full logic stack.

Preserve:
- fallback safety
- guardrails
- lead completion behavior
- session writes
  </action>
</task>

<task>
  <name>Implement faithful shadow simulation</name>
  <action>
Replace the proposal-only shadow prompt path with a simulate-mode call to the same shared runtime.

Shadow should produce:
- accepted slots
- rejected slots and reasons
- next state
- tool / action decisions
- comparison status vs production
  </action>
</task>

<task>
  <name>Fix out-of-order slot persistence</name>
  <action>
Ensure a single valid non-current slot is retained when the user provides it mid-flow, including cases like:
- location while phone is still missing
- schedule while service is still pending
- optional fields supplied early

The runtime should store valid data and ask only the next missing required field.
  </action>
</task>

<task>
  <name>Strengthen wrong-answer rejection and repair</name>
  <action>
Use centralized validation so clearly invalid or ambiguous values are not persisted.

Include targeted fixes for known extraction quality issues, including false-positive name capture from phrases such as `I am in Koramangala`.
  </action>
</task>

<task>
  <name>Expand parity logging for inspection</name>
  <action>
Log enough structured data so LLM I/O and PM inspection can compare:
- user input
- extracted candidates
- accepted slots
- rejected slots
- production next state
- shadow next state
- parity result
  </action>
</task>

</tasks>

<verification>
- maid_hire works through the shared runtime
- shadow calls shared runtime in simulate mode
- out-of-order slot tests pass
- false-positive name extraction regression test exists
- parity logs capture accepted and rejected slot detail
</verification>

<success_criteria>
- single-slot and multi-slot out-of-order messages are retained correctly
- shadow parity reflects the same runtime contract as production
- invalid values trigger repair instead of silent state corruption
- PM can inspect richer per-turn parity details
</success_criteria>
