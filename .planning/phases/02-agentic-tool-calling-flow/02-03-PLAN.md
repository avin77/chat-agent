---
phase: 02-agentic-tool-calling-flow
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: false
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
  - FLOW-05
  - FLOW-06

must_haves:
  truths:
    - "Supabase migration applied — conversation_sessions.agentic_mode column exists in production"
    - "With USE_AGENTIC=true: maid hire flow uses tool-calling handler, collects fields conversationally"
    - "With USE_AGENTIC=false: deterministic state machine works identically to before (no regression)"
    - "Eval score ≥95% with whatever USE_AGENTIC setting is active"
    - "After a completed agentic conversation, leads table shows collected_via='agentic'"
    - "agentic_mode=true appears in conversation_sessions for agentic turns"
  artifacts:
    - path: "supabase-migration-phase2.sql"
      provides: "migration SQL for human to run"
  key_links:
    - from: "Supabase SQL Editor"
      to: "conversation_sessions.agentic_mode"
      via: "human runs supabase-migration-phase2.sql"
---

<objective>
Human verification checkpoint: apply the Supabase migration, test the agentic flow end-to-end, and run the eval suite to confirm ≥95% pass rate.

Purpose: Validates Phase 2 is production-ready. The Supabase migration requires human action (SQL Editor). Functional testing confirms the feature flag routing, tool-calling behavior, and guardrails all work correctly in the live environment.

Output: Confirmed working agentic flow with migration applied.
</objective>

<execution_context>
@C:/Users/shobh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-agentic-tool-calling-flow/02-CONTEXT.md
@.planning/phases/02-agentic-tool-calling-flow/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy</name>
  <files></files>
  <action>
Run production build and deploy to Vercel so the agentic code is live. No source files are modified by this task — it only verifies the build and triggers deployment.

```bash
cd C:/Coding/EzyBot/ezybot
npm run build
```

If build passes, deploy:
```bash
bash deploy.sh "feat(phase-02): agentic tool-calling flow behind USE_AGENTIC flag"
```

Note: USE_AGENTIC is NOT yet set in Vercel env vars — deployment runs with deterministic path until human enables it. That is correct and safe.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build` exits 0 with no TypeScript or Next.js errors
    - Code deployed to Vercel (deterministic path still active — USE_AGENTIC not yet set)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply Supabase migration</name>
  <files>supabase-migration-phase2.sql</files>
  <action>
Human applies `supabase-migration-phase2.sql` in Supabase SQL Editor. Adds `agentic_mode BOOLEAN DEFAULT false` column to `conversation_sessions` table. Claude cannot access the Supabase dashboard directly — this is a genuine human-only step (no CLI available for direct Supabase schema changes).
  </action>
  <what-built>supabase-migration-phase2.sql — adds agentic_mode column to conversation_sessions</what-built>
  <how-to-verify>
1. Open Supabase Dashboard → your EzyBot project → SQL Editor
2. Copy and paste the full contents of `supabase-migration-phase2.sql` from the project root
3. Click "Run" — should complete successfully with no errors
4. Verify: in Supabase Table Editor → conversation_sessions → check that `agentic_mode` column appears (boolean, default false)
5. Type "migration applied" to continue
  </how-to-verify>
  <verify>Human confirms agentic_mode column appears in conversation_sessions table in Supabase Table Editor</verify>
  <done>agentic_mode column exists in conversation_sessions with BOOLEAN DEFAULT false</done>
  <resume-signal>Type "migration applied" once you've confirmed the agentic_mode column exists in Supabase</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Test agentic flow and run eval</name>
  <files>src/flows/agenticMaidHire.ts</files>
  <action>
Human tests the agentic flow end-to-end with USE_AGENTIC=true in .env.local, verifies Supabase data, checks regression with USE_AGENTIC=false, and runs eval:state to confirm ≥95% pass rate.
  </action>
  <what-built>Full Phase 2 agentic flow: handleMaidHireAgentic() + USE_AGENTIC routing + migration applied</what-built>
  <how-to-verify>
**Step 1: Enable agentic mode for testing**

Add to `.env.local`:
```
USE_AGENTIC=true
```
Then restart dev server: `npm run dev`

**Step 2: Test basic agentic conversation**

Open `http://localhost:3000` and start a maid hire conversation:
- Type: "I need to hire a maid"
- Expected: Bot asks for phone naturally (not using the rigid state machine phrasing)
- Type a 10-digit number (e.g., 9876543210)
- Expected: Bot acknowledges and asks for location
- Type: "Koramangala"
- Expected: Bot acknowledges and asks for service type
- Continue through service + schedule fields
- Expected: After all 4 required fields, bot confirms and triggers escalation

**Step 3: Verify Supabase data**

In Supabase Table Editor:
- `conversation_sessions` → find your test session → confirm `agentic_mode = true`
- `leads` → find your test lead → confirm `collected_via = 'agentic'`

**Step 4: Test feature flag off (regression check)**

Remove `USE_AGENTIC=true` from `.env.local` (or set to `false`), restart:
- Start a new maid hire conversation
- Expected: IDENTICAL behavior to before Phase 2 (deterministic phrasing)

**Step 5: Run eval**
```bash
npm run eval:state
```
Expected: ≥95% pass rate (same as before Phase 2)

**Step 6: Test force-escalate (optional)**

With USE_AGENTIC=true, in a new conversation:
- When asked for phone, type: "abc" three times in a row
- Expected after 3rd attempt: "Our team is standing by. Call us directly or try again shortly."
  </how-to-verify>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npm run eval:state 2>&1 | tail -10</automated>
  </verify>
  <done>Eval ≥95% with USE_AGENTIC=true, agentic_mode=true in Supabase, leads collected_via='agentic', regression confirmed clean</done>
  <resume-signal>Type "verified" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 2 complete when:
- [ ] `supabase-migration-phase2.sql` applied — agentic_mode column confirmed in Supabase
- [ ] USE_AGENTIC=true routes to agentic handler (observable in Supabase: agentic_mode=true)
- [ ] USE_AGENTIC=false/unset → deterministic path, no regression
- [ ] leads.collected_via = 'agentic' for completed agentic sessions
- [ ] eval:state passes ≥95%
- [ ] Force-escalate fires after 3 consecutive failed tool calls
</verification>

<success_criteria>
- All 6 FLOW requirements verified in production:
  - FLOW-01: 8 tools implemented and callable
  - FLOW-02: USE_AGENTIC flag routes correctly
  - FLOW-03: Supabase session saved with agentic_mode=true
  - FLOW-04: guardrails applied to agentic responses
  - FLOW-05: force-escalate after 3 consecutive failures
  - FLOW-06: loop detection sets __loop_detected, deterministic fallback activates
- Eval score ≥95% (production-ready)
- PHASE 2 COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/02-agentic-tool-calling-flow/02-03-SUMMARY.md`

Update STATE.md:
- Phase 2 status → COMPLETE
- Add session log entry for Phase 2 completion
</output>
