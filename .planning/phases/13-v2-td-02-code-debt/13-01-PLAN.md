# Phase 13: V2-TD-02 Code Debt Clearance - Plan

**Phase ID:** 13
**Milestone:** v2.0 Tech Debt
**Goal:** Clean up code artifacts identified in v2.0 milestone audit.

## Tasks

### 1. Remove obsolete agenticTools export
- File: `src/flows/agenticMaidHire.ts`
- Action: Remove `export const agenticTools` if it's no longer used.

### 2. Remove stray comment in route.ts
- File: `src/app/api/chat/route.ts`
- Action: Remove stray backslash comment at or around line 694.

### 3. Review dashboard gate conditions
- Check if `PreProdChecklist` in `src/app/dashboard/page.tsx` still has hardcoded `pass: true`.
- If appropriate, wire them to the actual eval scores or metrics.

## Exit Criteria
1. Obsolete code and stray comments removed.
2. `gsd-audit-milestone` no longer reports these code debt items.
