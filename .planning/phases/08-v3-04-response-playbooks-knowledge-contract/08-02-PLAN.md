---
phase: 08-v3-04-response-playbooks-knowledge-contract
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/lib/prompts-enhanced.ts
  - src/app/api/chat/route.ts
  - src/flows/MaidHiringFlow.ts
  - src/flows/agenticMaidHire.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Prompt builders consume the playbook contract instead of duplicating per-intent rules as freehand prose"
    - "Canonical maid_registration naming is used throughout runtime-facing code"
    - "maid_hire still respects its structured flow while sourcing required/optional/completion rules from the shared contract"
    - "Existing fallback and escalation behavior remains safe while becoming contract-driven"
  artifacts:
    - path: "src/lib/prompts-enhanced.ts"
      provides: "Playbook-driven prompt construction for complaint, maid_registration, and general"
      contains: "imports from responsePlaybooks"
    - path: "src/app/api/chat/route.ts"
      provides: "Canonical intent normalization and contract-aware non-maid-hire flow handling"
      contains: "normalizeIntentId"
    - path: "src/flows/agenticMaidHire.ts"
      provides: "maid_hire completion and repair logic aligned to the shared playbook contract"
      contains: "getResponsePlaybook('maid_hire')"
  key_links:
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/responsePlaybooks.ts"
      via: "intent normalization and playbook lookup"
      pattern: "normalizeIntentId"
    - from: "src/lib/prompts-enhanced.ts"
      to: "src/lib/responsePlaybookFormatter.ts"
      via: "prompt assembly from contract data"
      pattern: "formatPlaybookForPrompt"
---

<objective>
Replace Phase 8's current ad hoc prompt/runtime contract with playbook-driven wiring.

Purpose: make the live code consume the canonical playbook registry so roadmap decisions, prompt rules, and runtime behavior no longer drift apart.

Output:
- playbook-driven prompt generation
- canonical intent normalization in the chat route
- maid-hire repair/completion behavior aligned to the same contract source
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-CONTEXT.md
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-RESEARCH.md
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-01-PLAN.md
@src/lib/prompts-enhanced.ts
@src/app/api/chat/route.ts
@src/flows/MaidHiringFlow.ts
@src/flows/agenticMaidHire.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor prompts-enhanced to consume the playbook registry</name>
  <files>src/lib/prompts-enhanced.ts</files>
  <action>
Refactor `src/lib/prompts-enhanced.ts` so the file stops being the only source of truth for complaint / helper registration / general behavior.

Requirements:
- import the Phase 8 playbook registry and formatter helpers
- expose prompts under canonical keys (`maid_registration`, not just `helper_reg`)
- preserve current safety constraints:
  - English-only output
  - no prices
  - no external links
  - concise responses
- build required/optional-field and escalation language from playbook data instead of hardcoding it separately for each intent

If backward compatibility is needed, allow `helper_reg` lookups to resolve to the `maid_registration` playbook-generated prompt while keeping one contract source.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>prompts-enhanced consumes playbook data, canonical keys are available, and helper_reg no longer exists as a separate contract definition</done>
</task>

<task type="auto">
  <name>Task 2: Normalize route-level intent handling around the shared contract</name>
  <files>src/app/api/chat/route.ts</files>
  <action>
Update `src/app/api/chat/route.ts` so route-level intent handling uses the canonical playbook contract consistently.

Requirements:
- normalize any legacy `helper_reg` values to `maid_registration` at runtime-facing decision points
- keep DB table writes and existing storage semantics stable unless a safe compatibility shim is required
- use playbook lookups for non-maid-hire fallback / escalation language where appropriate
- remove any remaining prompt-branch assumptions that treat `helper_reg` as the primary intent id

Do not broaden scope into a new orchestration system. This task is about contract alignment and safe naming consistency.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npm run lint</automated>
  </verify>
  <done>route.ts uses canonical intent ids, compatibility is preserved, and non-maid-hire flow selection no longer depends on helper_reg as the main contract name</done>
</task>

<task type="auto">
  <name>Task 3: Align maid_hire flow logic to the shared playbook definitions</name>
  <files>src/flows/MaidHiringFlow.ts, src/flows/agenticMaidHire.ts</files>
  <action>
Update the maid-hire flow code so the shared Phase 8 contract also governs `maid_hire`, not just prompt-only intents.

Requirements:
- source required vs optional field definitions from `getResponsePlaybook('maid_hire')`
- keep current flow order and guardrails intact
- align completion confirmation and repair copy to the playbook contract wherever feasible without rewriting the whole runtime
- ensure optional fields remain optional and required fields remain blocking, matching the roadmap contract exactly

This task should reduce duplicate contract knowledge, not destabilize the working state-machine behavior.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node scripts/check-response-playbooks.js</automated>
  </verify>
  <done>maid_hire flow logic reads shared contract definitions for required/optional field semantics and still preserves the current safe execution path</done>
</task>

</tasks>

<verification>
- prompt generation imports playbook data
- route-level intent handling is canonicalized around maid_registration
- maid_hire required/optional behavior remains aligned to the shared contract
- TypeScript and lint pass after integration
</verification>

<success_criteria>
- prompt templates now reference playbook contracts instead of drifting prose blobs
- helper_reg survives only as a compatibility alias, not a separate behavioral contract
- all major runtime branches can point at the same Phase 8 contract source
</success_criteria>

<output>
After completion, create `.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-02-SUMMARY.md`
</output>
