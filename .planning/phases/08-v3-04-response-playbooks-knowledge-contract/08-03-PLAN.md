---
phase: 08-v3-04-response-playbooks-knowledge-contract
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - package.json
  - docs/response-playbooks.md
  - data/playbook-golden-dataset.json
  - scripts/generate-playbook-reference.js
  - scripts/eval-playbooks.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "PM has one readable playbook reference artifact for all canonical intents"
    - "Playbook behavior is testable through a dedicated dataset and evaluator"
    - "Docs and eval coverage are generated from or directly aligned with the same contract source"
    - "Phase 8 exit criteria can be checked without relying only on generic state-machine evals"
  artifacts:
    - path: "docs/response-playbooks.md"
      provides: "PM-facing knowledge contract for all canonical intents"
      contains: "required fields, optional fields, repair behavior, completion confirmation, escalation criteria"
    - path: "data/playbook-golden-dataset.json"
      provides: "Contract-focused eval cases for entry, repair, optional handling, and completion"
      contains: "maid_hire, complaint, maid_registration, general"
    - path: "scripts/eval-playbooks.js"
      provides: "Dedicated playbook evaluator against /api/chat"
      contains: "contract assertions by intent"
  key_links:
    - from: "scripts/generate-playbook-reference.js"
      to: "src/lib/responsePlaybooks.ts"
      via: "generated docs from canonical contract"
      pattern: "RESPONSE_PLAYBOOKS"
    - from: "scripts/eval-playbooks.js"
      to: "data/playbook-golden-dataset.json"
      via: "dataset-driven contract verification"
      pattern: "playbook-golden-dataset"
---

<objective>
Make Phase 8 reviewable and testable for PM and engineering.

Purpose: turn the playbook contract into a readable reference and a dedicated eval surface so future prompt changes cannot silently drift from the intended behavior.

Output:
- generated or synchronized PM reference docs
- playbook golden dataset
- playbook evaluator and npm scripts
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-CONTEXT.md
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-RESEARCH.md
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-01-PLAN.md
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-02-PLAN.md
@package.json
@scripts/eval-state-machine.js
@data/state-golden-dataset.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate a PM-readable response playbook reference</name>
  <files>scripts/generate-playbook-reference.js, docs/response-playbooks.md</files>
  <action>
Create a small generation flow so PM and engineering can review the contract without manually duplicating it.

Requirements:
- add `scripts/generate-playbook-reference.js`
- generate or refresh `docs/response-playbooks.md` from the canonical playbook registry
- the document must include, per intent:
  - canonical intent name
  - aliases (if any)
  - required fields
  - optional fields
  - entry confirmation behavior
  - repair / failure behavior
  - completion confirmation format
  - escalation criteria

Prefer generated docs over handwritten duplication so Phase 8 does not introduce a second drifting source of truth.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node scripts/generate-playbook-reference.js</automated>
  </verify>
  <done>docs/response-playbooks.md is present and can be regenerated from the contract source without manual editing</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated playbook golden dataset</name>
  <files>data/playbook-golden-dataset.json</files>
  <action>
Create `data/playbook-golden-dataset.json` with contract-oriented conversation cases that specifically test Phase 8 behavior.

Coverage requirements:
- `maid_hire`: entry confirmation, required-field progression, optional-field skip, completion confirmation
- `complaint`: issue intake, callback/contact handling, severity capture, incident timing optionality
- `maid_registration`: registration entry, role/service capture, availability and preferred areas
- `general`: answer-first behavior without forcing unnecessary slot collection
- at least one repair case per non-general intent
- at least one canonical-alias case proving helper_reg-style requests still land on maid_registration behavior

Keep the dataset focused on contract assertions rather than duplicating the entire state-machine dataset.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node -e "const data=require('./data/playbook-golden-dataset.json'); console.log(data.length)"</automated>
  </verify>
  <done>a playbook dataset exists with representative cases for all canonical intents and contract behaviors</done>
</task>

<task type="auto">
  <name>Task 3: Add playbook eval tooling and package scripts</name>
  <files>scripts/eval-playbooks.js, package.json</files>
  <action>
Add a dedicated evaluator for the Phase 8 contract and expose it through npm scripts.

Requirements:
- create `scripts/eval-playbooks.js`
- allow `--url=` override like the existing eval runner
- read `data/playbook-golden-dataset.json`
- assert contract behaviors such as:
  - correct entry confirmation
  - asking for missing required data
  - optional field acceptance or skip behavior
  - expected completion confirmation and escalation conditions
  - answer-first behavior for general intent
- add npm scripts in `package.json`:
  - `docs:playbooks`
  - `eval:playbooks`
  - optional `eval:playbooks:prod` if consistent with existing style

Reuse existing eval runner patterns where practical, but keep the assertions contract-specific.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node scripts/eval-playbooks.js --dataset=data/playbook-golden-dataset.json --dry-run</automated>
  </verify>
  <done>playbook eval tooling exists, package scripts are wired, and the dataset can be checked without hand-running assertions</done>
</task>

</tasks>

<verification>
- PM reference doc exists and is regeneratable
- playbook dataset covers all canonical intents
- dedicated playbook eval runner exists
- package.json exposes scripts for docs generation and playbook evals
</verification>

<success_criteria>
- PM has one place that defines what good completion means per intent
- Phase 8 behavior is testable through a dedicated dataset and evaluator
- future prompt changes can be checked against the contract instead of subjective review only
</success_criteria>

<output>
After completion, create `.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-03-SUMMARY.md`
</output>
