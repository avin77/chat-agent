---
phase: 08-v3-04-response-playbooks-knowledge-contract
verified: 2026-03-11T00:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
human_verification:
  - test: "Run `npm run eval:playbooks` against a live local server once the `.next` file lock is cleared."
    expected: "All 8 playbook scenarios pass against `/api/chat` with answer-first, registration, complaint, and maid-hire contract coverage."
    why_human: "The live evaluator needs a running app instance; current `npm run build` is blocked by a local `.next` artifact lock, not by a TypeScript or contract error."
notes:
  - "`npm run build` failed with `EPERM: operation not permitted, unlink '.next/build/chunks/node_modules_fe693df6._.js'`. TypeScript, playbook checks, docs generation, and playbook dry-run all passed."
---

# Phase 8: Response Playbooks (Knowledge Contract) - Verification Report

**Phase Goal:** Define what information is minimally required and how to respond for each intent, then make that contract reusable by prompts and eval tooling.  
**Verified:** 2026-03-11  
**Status:** PASSED  
**Re-verification:** No - initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every canonical intent has an explicit playbook contract | VERIFIED | `src/lib/responsePlaybooks.ts` defines `maid_hire`, `complaint`, `maid_registration`, and `general` |
| 2 | Legacy `helper_reg` is normalized to `maid_registration` | VERIFIED | `normalizeIntentId()` maps aliases to canonical ids and route.ts now calls it in runtime session handling |
| 3 | Prompt templates consume the playbook contract instead of remaining fully ad hoc | VERIFIED | `src/lib/prompts-enhanced.ts` builds prompts from `formatPlaybookForPrompt()` and `getResponsePlaybook()` |
| 4 | The non-maid-hire runtime path uses canonical `maid_registration` routing | VERIFIED | `src/app/api/chat/route.ts` switched primary branching and escalation handling from `helper_reg` to `maid_registration` |
| 5 | Maid-hire required/optional field semantics are sourced from the shared contract | VERIFIED | `src/flows/MaidHiringFlow.ts` and `src/flows/agenticMaidHire.ts` import maid-hire playbook constants |
| 6 | PM has a readable reference document generated from the contract | VERIFIED | `docs/response-playbooks.md` is generated by `scripts/generate-playbook-reference.js` |
| 7 | Playbook coverage is represented by a dedicated dataset | VERIFIED | `data/playbook-golden-dataset.json` contains 8 scenarios across maid_hire, complaint, maid_registration, and general |
| 8 | Phase 8 contract checks are executable in code | VERIFIED | `scripts/check-response-playbooks.js --stage=full` passed and `scripts/eval-playbooks.js --dry-run` validated the dataset |

**Score:** 8/8 truths verified

---

## Verification Commands

| Command | Result |
|---------|--------|
| `node scripts/check-response-playbooks.js --stage=full` | PASSED |
| `npx tsc --noEmit --project tsconfig.json` | PASSED |
| `node --experimental-strip-types scripts/eval-playbooks.js --dry-run` | PASSED (`8 scenarios`, `maid_hire=3`, `complaint=2`, `maid_registration=2`, `general=1`) |
| `npm run docs:playbooks` | PASSED (`docs/response-playbooks.md` regenerated) |
| `npm run build` | BLOCKED by local `.next` file lock (`EPERM unlink`) |

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/responsePlaybooks.ts` | Canonical playbook registry + alias normalization | VERIFIED | Exports playbooks, field metadata, `normalizeIntentId()`, and lookup helpers |
| `src/lib/responsePlaybookFormatter.ts` | Prompt/doc formatter helpers | VERIFIED | Exports `formatPlaybookForPrompt()` and `formatPlaybookForDocs()` |
| `src/lib/prompts-enhanced.ts` | Playbook-driven prompts | VERIFIED | Rewritten to consume the shared contract and expose `maid_registration` |
| `src/app/api/chat/route.ts` | Canonical registration routing | VERIFIED | Normalizes runtime intent ids and stores registration leads through the canonical path |
| `src/flows/MaidHiringFlow.ts` | Shared maid-hire contract usage | VERIFIED | Required/optional semantics now point at the shared playbook ids |
| `src/flows/agenticMaidHire.ts` | Shared maid-hire contract usage | VERIFIED | Required/optional arrays and completion messaging now derive from the playbook module |
| `scripts/check-response-playbooks.js` | Contract checker | VERIFIED | Supports `contract`, `integration`, and `full` validation stages |
| `scripts/generate-playbook-reference.js` | PM-doc generator | VERIFIED | Generates `docs/response-playbooks.md` from the contract source |
| `scripts/eval-playbooks.js` | Playbook evaluator | VERIFIED | Supports dataset validation via `--dry-run` and live `/api/chat` mode |
| `data/playbook-golden-dataset.json` | Contract-focused dataset | VERIFIED | Covers maid-hire entry/skip/completion, complaint, registration, alias normalization, and general answer-first behavior |

---

## Human Verification Required

### 1. Live Playbook Eval Run

**Test:** Run `npm run eval:playbooks` against a live local or deployed app  
**Expected:** All 8 scenarios pass using the new playbook contract  
**Why human:** Requires a running `/api/chat` server and live LLM responses

### 2. End-to-End Registration Path

**Test:** Start a fresh chat with a registration-style prompt such as "I want to register for maid work"  
**Expected:** The bot follows the maid-registration playbook rather than falling back to a generic flow  
**Why human:** Confirms the canonical routing change in the live runtime

---

## Gaps Summary

No code or contract gaps were found in the Phase 8 scope.

The only blocked verification step is a fresh production build because `next build` could not remove a locked file under `.next/build/chunks/`. That is an environment artifact issue, not a TypeScript or playbook-contract issue.
