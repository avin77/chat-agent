---
phase: 08-v3-04-response-playbooks-knowledge-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/responsePlaybooks.ts
  - src/lib/responsePlaybookFormatter.ts
  - scripts/check-response-playbooks.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "There is one canonical playbook registry for maid_hire, complaint, maid_registration, and general"
    - "Required fields and optional fields are explicit instead of buried in prompt prose"
    - "Legacy helper_reg is treated as an alias, not the primary contract id"
    - "The registry captures entry, repair, completion, and escalation behavior per intent"
  artifacts:
    - path: "src/lib/responsePlaybooks.ts"
      provides: "Canonical response playbook registry and intent normalization helpers"
      exports: ["RESPONSE_PLAYBOOKS", "normalizeIntentId", "getResponsePlaybook"]
    - path: "src/lib/responsePlaybookFormatter.ts"
      provides: "Pure helpers that turn playbook definitions into prompt-ready sections and PM-readable text"
      exports: ["formatPlaybookForPrompt", "formatPlaybookForDocs"]
    - path: "scripts/check-response-playbooks.js"
      provides: "Repo-local contract validation for playbook completeness and field coverage"
      contains: "required vs optional coverage checks"
  key_links:
    - from: "src/lib/responsePlaybooks.ts"
      to: "src/lib/prompts-enhanced.ts"
      via: "shared contract import in later plans"
      pattern: "getResponsePlaybook"
    - from: "src/lib/responsePlaybookFormatter.ts"
      to: "docs/response-playbooks.md"
      via: "shared formatting source for generated docs"
      pattern: "formatPlaybookForDocs"
---

<objective>
Create the canonical Phase 8 contract artifact before touching prompt/runtime wiring.

Purpose: move intent response rules out of scattered prose and into one machine-readable registry that downstream prompts, evals, and PM docs can all share.

Output:
- `src/lib/responsePlaybooks.ts`
- `src/lib/responsePlaybookFormatter.ts`
- `scripts/check-response-playbooks.js`
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-CONTEXT.md
@.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-RESEARCH.md
@src/lib/prompts-enhanced.ts
@src/app/api/chat/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the canonical response playbook registry</name>
  <files>src/lib/responsePlaybooks.ts</files>
  <action>
Create `src/lib/responsePlaybooks.ts` as the single contract source for response behavior.

Required structure:
- a canonical intent union covering `maid_hire`, `complaint`, `maid_registration`, and `general`
- a registry object or array where each playbook defines:
  - `intent`
  - `aliases` (include `helper_reg` for compatibility under `maid_registration`)
  - `requiredFields`
  - `optionalFields`
  - `entryConfirmation`
  - `repairRules`
  - `completionRule`
  - `completionConfirmation`
  - `escalationCriteria`
  - `notes` or `answerFirstPolicy` for FAQ/general behavior where needed

The content must reflect the roadmap minimum-data contract:
- `maid_hire` required: `phone`, `area`, `service_type`, `schedule`
- `complaint` required: `contact`, `issue_summary`, `severity`, `callback_preference`
- `complaint` optional: `incident_timing`
- `maid_registration` required: `contact`, `role_service_offered`, `experience`, `availability_window`, `preferred_areas`
- `general` should still define response behavior even if it has no required collection schema

Export:
- `RESPONSE_PLAYBOOKS`
- `type CanonicalIntentId`
- `type ResponsePlaybook`
- `normalizeIntentId(intent: string): CanonicalIntentId`
- `getResponsePlaybook(intent: string): ResponsePlaybook`

Keep this file pure data plus pure lookup helpers. No DB access, no prompt strings copied wholesale from current prose blobs.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node scripts/check-response-playbooks.js</automated>
  </verify>
  <done>`responsePlaybooks.ts` exists, all four canonical intents are present, helper_reg resolves to maid_registration, and roadmap-required fields are explicit in the registry</done>
</task>

<task type="auto">
  <name>Task 2: Add playbook formatting helpers for prompts and docs</name>
  <files>src/lib/responsePlaybookFormatter.ts</files>
  <action>
Create `src/lib/responsePlaybookFormatter.ts` with pure helpers that render the playbook registry into downstream-friendly text structures.

Include:
- `formatPlaybookForPrompt(playbook)` -> concise prompt-ready sections for required fields, optional fields, repair rules, completion, and escalation
- `formatPlaybookForDocs(playbook)` -> markdown-ready representation for PM docs and review artifacts

Formatting helpers must not become a second source of truth. They should only transform registry data.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>formatter helpers compile and can render every playbook without branching on helper_reg-specific prose</done>
</task>

<task type="auto">
  <name>Task 3: Add a repo-local playbook integrity checker</name>
  <files>scripts/check-response-playbooks.js</files>
  <action>
Create `scripts/check-response-playbooks.js` as a lightweight Node script that imports or reads the playbook registry and fails loudly when the contract drifts.

Checks must include:
- all canonical intents exist
- required and optional fields are arrays and do not overlap
- `maid_registration` exposes `helper_reg` as an alias
- every non-general intent has completion and escalation metadata
- roadmap-required fields for Phase 8 are present exactly where expected

The script should print a short pass/fail summary and exit with code 1 on any contract gap.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node scripts/check-response-playbooks.js</automated>
  </verify>
  <done>the checker passes locally and would fail if a required field or canonical intent were removed</done>
</task>

</tasks>

<verification>
- canonical playbook registry exists and is importable
- helper_reg is normalized to maid_registration
- required and optional fields are explicit for all non-general intents
- playbook integrity checker passes
- TypeScript still compiles after adding the registry and formatters
</verification>

<success_criteria>
- one contract source now defines response/data expectations for all canonical intents
- downstream code can import playbook data without copying prompt prose
- Phase 8 minimum-data requirements are encoded in code, not just in roadmap text
</success_criteria>

<output>
After completion, create `.planning/phases/08-v3-04-response-playbooks-knowledge-contract/08-01-SUMMARY.md`
</output>
