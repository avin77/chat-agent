---
phase: 11-v3-07-flywheel-for-synonyms-and-recovery
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/lib/serviceVocabulary.ts
  - src/extractors/dataExtractor.ts
  - src/lib/prompts-enhanced.ts
  - src/lib/responsePlaybooks.ts
  - src/test/test-service-vocabulary.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Accepted synonym and recovery fixes land in one explicit knowledge source instead of being scattered across extractor and prompt heuristics"
    - "The c56-style Hinglish synonym family can be addressed through a reviewable vocabulary update"
    - "Prompts and playbooks reference the same canonical vocabulary that extractor logic uses"
    - "New synonym additions are covered by focused tests"
  artifacts:
    - path: "src/lib/serviceVocabulary.ts"
      provides: "Canonical service-type and recovery synonym vocabulary"
      exports: ["SERVICE_VOCABULARY", "normalizeServicePhrase"]
    - path: "src/extractors/dataExtractor.ts"
      provides: "Extractor path updated to consume shared vocabulary"
      contains: "normalizeServicePhrase"
    - path: "src/lib/prompts-enhanced.ts"
      provides: "Prompt hints aligned with the same canonical vocabulary"
      contains: "Hinglish"
    - path: "src/lib/responsePlaybooks.ts"
      provides: "Playbook examples aligned with accepted synonym classes"
      contains: "service_type"
  key_links:
    - from: "src/extractors/dataExtractor.ts"
      to: "src/lib/serviceVocabulary.ts"
      via: "shared service normalization"
      pattern: "normalizeServicePhrase"
    - from: "src/lib/prompts-enhanced.ts"
      to: "src/lib/serviceVocabulary.ts"
      via: "prompt hint generation"
      pattern: "SERVICE_VOCABULARY"
---

<objective>
Convert mined synonym and recovery findings into one reusable knowledge source.

Purpose: stop fixing synonym misses only in one layer and instead make extractor, prompt, and playbook behavior converge on the same accepted vocabulary.

Output:
- canonical service vocabulary module
- extractor integration
- prompt and playbook alignment
- focused synonym tests
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-v3-07-flywheel-for-synonyms-and-recovery/11-RESEARCH.md
@src/extractors/dataExtractor.ts
@src/lib/prompts-enhanced.ts
@src/lib/responsePlaybooks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a canonical vocabulary module for service and recovery phrases</name>
  <files>src/lib/serviceVocabulary.ts</files>
  <action>
Create `src/lib/serviceVocabulary.ts` as the explicit source of truth for accepted service-type phrases and recovery-oriented examples.

Requirements:
- represent canonical service values already used by the product
- include initial synonym families for phrases already known in the repo and roadmap, including the `c56` Hinglish class
- expose helpers that normalize a free-text phrase to a canonical service type when possible
- keep the data structure easy to extend from future mined artifacts

Do not add Supabase or UI coupling here.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>a reusable vocabulary module exists for accepted synonym classes and canonical service normalization</done>
</task>

<task type="auto">
  <name>Task 2: Replace scattered synonym logic with the shared vocabulary</name>
  <files>src/extractors/dataExtractor.ts, src/lib/prompts-enhanced.ts, src/lib/responsePlaybooks.ts</files>
  <action>
Update the extractor, prompts, and playbooks to use the shared vocabulary.

Requirements:
- `src/extractors/dataExtractor.ts`
  - route service-type normalization through the vocabulary helper instead of hardcoding all synonym checks inline
- `src/lib/prompts-enhanced.ts`
  - align prompt hints and recovery examples with the canonical vocabulary
- `src/lib/responsePlaybooks.ts`
  - ensure service-type examples and repair guidance reference the same accepted phrases

Keep existing canonical output values unchanged unless a deliberate migration is required.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <done>extractor, prompt, and playbook layers all reference the same vocabulary instead of maintaining separate synonym lists</done>
</task>

<task type="auto">
  <name>Task 3: Add focused synonym normalization tests</name>
  <files>src/test/test-service-vocabulary.ts</files>
  <action>
Add focused tests for the vocabulary and extractor integration.

Cover at minimum:
- known English service phrases
- known Hinglish or synonym phrases related to the c56 class
- unsupported phrases remain null rather than mapping incorrectly
- updates do not regress existing canonical service mappings

Use the existing lightweight Node-based test style.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node --experimental-strip-types src/test/test-service-vocabulary.ts</automated>
  </verify>
  <done>the shared vocabulary and its integration are protected by focused regression tests</done>
</task>

</tasks>

<verification>
- one service vocabulary module exists
- extractor, prompts, and playbooks consume the shared vocabulary
- c56-like synonym classes have a clear place to be fixed
- focused tests cover new and existing mappings
</verification>

<success_criteria>
- synonym fixes become reviewable code changes instead of scattered heuristics
- accepted vocabulary can be extended from mined artifacts without reworking multiple files each time
- Phase 11 has a concrete path from mined miss to shipped fix
</success_criteria>

<output>
After completion, create `.planning/phases/11-v3-07-flywheel-for-synonyms-and-recovery/11-02-SUMMARY.md`
</output>
