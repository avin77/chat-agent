---
phase: 11-v3-07-flywheel-for-synonyms-and-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mine-missed-extractions.js
  - scripts/mine-golden-from-prod.js
  - scripts/analyze-guardrail-mods.js
  - scripts/lib/flywheelUtils.js
  - package.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "The repo can mine synonym and recovery misses from production or eval-adjacent data into reviewable artifacts"
    - "Mining outputs are PII-safe and deterministic enough for repeated use"
    - "The missing FLY-01 through FLY-04 script layer is materially closed by this plan"
    - "The initial c56-style failure class can be isolated through generated artifacts instead of manual log reading"
  artifacts:
    - path: "scripts/mine-missed-extractions.js"
      provides: "Rejected or retried extraction pairs grouped by state or failure class"
      contains: "missed-extractions-YYYY-MM-DD"
    - path: "scripts/mine-golden-from-prod.js"
      provides: "PII-safe mined golden candidates from complete sessions"
      contains: "mined-golden-YYYY-MM-DD"
    - path: "scripts/analyze-guardrail-mods.js"
      provides: "Guardrail modification ranking by state or category"
      contains: "raw != after_guardrails"
    - path: "scripts/lib/flywheelUtils.js"
      provides: "Shared hashing, redaction, output naming, and argument parsing helpers"
      exports: ["hashPII", "writeDatedJson"]
  key_links:
    - from: "scripts/mine-missed-extractions.js"
      to: "scripts/lib/flywheelUtils.js"
      via: "shared artifact schema and redaction helpers"
      pattern: "writeDatedJson"
    - from: "scripts/mine-golden-from-prod.js"
      to: "package.json"
      via: "npm mine scripts"
      pattern: "mine"
---

<objective>
Create the mining layer for the Phase 11 flywheel.

Purpose: make repeated misses observable and reviewable through deterministic artifacts instead of ad hoc eval reading.

Output:
- three mining scripts
- shared flywheel script utilities
- npm entrypoints
</objective>

<execution_context>
@C:/Users/shobh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/shobh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-v3-07-flywheel-for-synonyms-and-recovery/11-RESEARCH.md
@package.json
@scripts/eval-state-machine.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared flywheel script utilities</name>
  <files>scripts/lib/flywheelUtils.js</files>
  <action>
Create `scripts/lib/flywheelUtils.js` with helpers used by all Phase 11 mining scripts.

The helper module should provide:
- deterministic dated output naming
- JSON writing under `data/`
- hashing or redaction helpers for phone numbers and names
- simple CLI argument parsing for `--out`, `--dry-run`, and `--limit`
- shared artifact metadata such as `generated_at`, `source`, and `schema_version`

Keep this file plain Node.js and dependency-free.
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node -e "const m=require('./scripts/lib/flywheelUtils.js'); console.log(typeof m.writeDatedJson)"</automated>
  </verify>
  <done>shared helpers exist so the mining scripts do not duplicate hashing, output naming, and artifact metadata logic</done>
</task>

<task type="auto">
  <name>Task 2: Implement the three mining scripts and package entrypoints</name>
  <files>scripts/mine-missed-extractions.js, scripts/mine-golden-from-prod.js, scripts/analyze-guardrail-mods.js, package.json</files>
  <action>
Implement the three missing mining scripts referenced in `.planning/REQUIREMENTS.md`.

Requirements:
- `scripts/mine-missed-extractions.js`
  - query or load sessions where `attempts > 0`
  - output rejected or retried message pairs grouped by state or miss class
- `scripts/mine-golden-from-prod.js`
  - reconstruct COMPLETE maid_hire sessions from logs or sessions
  - hash PII before writing artifacts
- `scripts/analyze-guardrail-mods.js`
  - group logs where raw output differs from post-guardrail output
  - rank states or categories by guardrail trigger frequency

All three scripts must:
- support `--dry-run` or `--help`
- fail clearly if required env vars are missing
- write deterministic JSON artifact shapes

Update `package.json` with at least:
- `mine`
- `mine:misses`
- `mine:golden`
- `mine:guardrails`
  </action>
  <verify>
    <automated>cd C:/Coding/EzyBot/ezybot && node scripts/mine-missed-extractions.js --help && node scripts/mine-golden-from-prod.js --help && node scripts/analyze-guardrail-mods.js --help</automated>
  </verify>
  <done>the repo has the missing flywheel mining scripts and package entrypoints with safe dry-run or help behavior</done>
</task>

</tasks>

<verification>
- all three mining scripts exist
- shared helper centralizes output naming and redaction
- package.json exposes mining entrypoints
- scripts can be exercised in help or dry-run mode without production data access
</verification>

<success_criteria>
- the repo can generate reviewable miss artifacts instead of relying on manual eval report reading
- pending FLY mining requirements are concretely addressed
- Phase 11 has a stable ingestion layer for synonym and recovery misses
</success_criteria>

<output>
After completion, create `.planning/phases/11-v3-07-flywheel-for-synonyms-and-recovery/11-01-SUMMARY.md`
</output>
